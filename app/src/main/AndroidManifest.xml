<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.techacademy.yuuwa.takano.matching">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AccountChange"></activity>
        <activity android:name=".AccountPageActivity" />
        <activity android:name=".AccountPageFragment" />
        <activity android:name=".QaActivity" />
        <activity android:name=".MatchingListFragment" />
        <activity android:name=".AllListFragment" />
        <activity android:name=".LocalListFragment" />
        <activity android:name=".LocalListActivity" />
        <activity android:name=".MatchingList" />
        <activity android:name=".AllActivity" />
        <activity android:name=".LocalActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>